package com.infosys.basic.util;


import java.io.File;
import java.io.IOException;

/**
 * 
 * <p>Description:吧图片进行压缩同时加上水印</p>
 * @version 1.0
 */
public class ResizeImage {

	public static void resizeAndCompressImage(String sourceDir,File sourceFile) throws IOException{
//		int width =400;
//		int height=-1;
		
//		String name = sourceFile.getName();
//		String ext = name.substring(name.lastIndexOf('.')+1);
////		System.out.println("ext:"+ext);
////		String nameWithoutext = name.substring(0,name.lastIndexOf('.'));
//		
////		System.out.println("nameWithoutext:"+nameWithoutext);
//		if("gif".equals(ext.toLowerCase())){
//			return;
//		}
//		File copyFile = new File(sourceDir,nameWithoutext+"old."+ext);
//		sourceFile.renameTo(copyFile);
		
		
		
		//File compressFile = new File(sourceDir, name);
//		ImageSizer.resize(copyFile, compressFile, width,height, ext);	
		//ImageUtils.pressText(compressFile.getAbsolutePath(), "@拇指帮", "宋体", Font.PLAIN, 20, Color.LIGHT_GRAY, -1, 400, 1f);	
		
		
	}
}
